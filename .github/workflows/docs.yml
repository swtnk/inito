name: Docs

# Build the Sphinx docs and publish them to GitHub Pages. Because the
# account's user site (swtnk.github.io) has the custom domain
# swetanksubham.com, this project site is served at
# https://swetanksubham.com/inito/ automatically — no CNAME file is needed
# in this repo.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One deployment at a time; let the newest push supersede an in-flight one so
# a stuck/overlapping deploy can't wedge the github-pages environment (the
# latest docs content should always win).
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    name: Build docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          python-version: "3.13"
      - name: Install project (dev extras)
        run: |
          uv venv --python 3.13
          uv pip install -e ".[dev]"
      - name: Build docs (warnings-as-errors)
        run: uv run sphinx-build -b html docs docs/_build -W
      - name: Disable Jekyll (keep _static/_sources dirs)
        run: touch docs/_build/.nojekyll
      - name: Configure Pages
        # Requires a one-time repo setup: Settings → Pages → Build and
        # deployment → Source → "GitHub Actions".
        uses: actions/configure-pages@v6
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs/_build

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
